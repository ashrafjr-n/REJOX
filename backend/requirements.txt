fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# AI Resolution Engine — Gemini via the official SDK. Imported lazily by
# GeminiProvider, so it is optional for offline runs / the test suite
# (FakeProvider needs nothing).
google-genai==1.15.0

# CLI — the terminal face of the pipeline (`rejox migrate …`).
typer==0.26.8
rich==13.9.4

# Durable job queue (REJOX_QUEUE=rq): migrations survive an API restart and
# scale across worker processes. Unused by the default `thread` backend.
rq==2.1.0
redis==5.2.1

# Dev / test
pytest==8.3.4
httpx==0.28.1
