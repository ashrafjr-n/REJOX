"""Planner stage.

Turns analyzer findings into an ordered, concrete migration plan and surfaces
the decisions that require human input (the "Ask" stage). Orchestrates the
sequence of downstream agents.
"""
