# Rejox frontend — environment configuration.
# Copy to .env for local development. Never commit .env.

# Base URL of the Rejox backend (FastAPI / uvicorn). The axios client in
# src/lib/api.ts reads this; if unset it falls back to http://127.0.0.1:8000.
VITE_API_URL=http://localhost:8000
