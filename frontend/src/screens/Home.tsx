/**
 * Temporary marketing-home placeholder mounted at `/`.
 * Pure-black full-screen with centered white text. Replaced in a later prompt.
 */
export function Home() {
  return (
    <div
      style={{
        minHeight: '100vh',
        background: '#050505',
        color: '#ffffff',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      Rejox home — coming soon
    </div>
  )
}
